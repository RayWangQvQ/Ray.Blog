@page "/tags"

@inherits BlogComponentBase

<Container>
@foreach (var item in this.TagList)
{
    <Badge Link="@($"tag/{item.Id}/posts")" Color="@GetRandomBadgeColor()" Margin="Margin.Is2">
        @(item.Alias)(@item.PostCount)
    </Badge>
}
</Container>
